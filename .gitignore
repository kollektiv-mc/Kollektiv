# Products, cloned by scripts/bootstrap.sh — each is its own repository.
Konnekt/
Kommands/

# Agent tooling: shared config is committed, personal and runtime state is not.
.claude/settings.local.json

# Python bytecode, from importing suite-check.py
__pycache__/

# OS
.DS_Store

# aislop writes a scan history beside its config on every run.
.aislop/history.jsonl
